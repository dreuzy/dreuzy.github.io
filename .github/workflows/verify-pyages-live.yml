name: Verify live PyAges software figure

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/verify-pyages-live.yml"
      - "logiciels.html"
      - "en/software.html"
      - "assets/pyages-software-figure.png"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Verify deployed PyAges software figure
    runs-on: ubuntu-latest
    steps:
      - name: Wait for GitHub Pages and verify image
        shell: bash
        run: |
          set -euo pipefail
          ok=0
          for i in $(seq 1 30); do
            echo "Attempt $i/30"
            if curl -fsSL -H 'Cache-Control: no-cache' "https://dreuzy.github.io/assets/pyages-software-figure.png?verify=${GITHUB_SHA}-${i}" -o /tmp/pyages-software.png; then
              size=$(stat -c%s /tmp/pyages-software.png)
              kind=$(file -b /tmp/pyages-software.png)
              echo "size=$size"
              echo "type=$kind"
              if [ "$size" -gt "100000" ] && echo "$kind" | grep -qi 'PNG image data'; then
                ok=1
                break
              fi
            fi
            sleep 5
          done
          test "$ok" = "1"

      - name: Verify French and English page references
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL -H 'Cache-Control: no-cache' "https://dreuzy.github.io/logiciels.html?verify=${GITHUB_SHA}" -o /tmp/fr.html
          curl -fsSL -H 'Cache-Control: no-cache' "https://dreuzy.github.io/en/software.html?verify=${GITHUB_SHA}" -o /tmp/en.html
          grep -F 'src="assets/pyages-software-figure.png"' /tmp/fr.html
          grep -F 'src="../assets/pyages-software-figure.png"' /tmp/en.html
