name: Update publications over time

on:
  workflow_dispatch:
  schedule:
    - cron: '17 0 1 1 *'
  push:
    branches: [main]
    paths:
      - 'bibliographie-payload-*.txt'
      - 'scripts/build_publications_over_time.py'
      - 'scripts/build_static_bibliography.py'
      - '.github/workflows/update-publications-over-time.yml'

permissions:
  contents: write

jobs:
  build:
    name: Rebuild publication history figures
    runs-on: ubuntu-latest
    steps:
      - name: Check out the site
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install plotting dependencies
        run: python -m pip install --quiet matplotlib beautifulsoup4
      - name: Rebuild bibliography, figures and data
        run: |
          python scripts/build_static_bibliography.py
          python scripts/build_publications_over_time.py
      - name: Validate local links
        run: python scripts/check_local_links.py
      - name: Commit regenerated figures when needed
        run: |
          if git diff --quiet -- bibliographie.html en/bibliography.html publications-au-fil-du-temps.svg en/publications-au-fil-du-temps.svg publications-au-fil-du-temps-data.json; then
            echo 'Bibliography and publication-history outputs are already up to date.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add bibliographie.html en/bibliography.html publications-au-fil-du-temps.svg en/publications-au-fil-du-temps.svg publications-au-fil-du-temps-data.json
          git commit -m 'Refresh bibliography and publication history'
          git push
